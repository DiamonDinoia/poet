# POET Detail Headers

The files contained in this directory expose internal implementation
building blocks that are **not** part of the supported public API. Their
interfaces may change without notice between releases. Downstream
consumers should avoid relying on these headers whenever possible and
prefer the umbrella include `<poet/poet.hpp>` instead.
